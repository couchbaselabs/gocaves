package crud

import (
	"github.com/couchbaselabs/gocaves/checks"
)

// CheckCCCPBasic does a basic check that the SDK can connect via CCCP
func CheckCCCPBasic(t *checks.T) {
}

// CheckHTTPBasic does a basic check that the SDK can connect via HTTP
func CheckHTTPBasic(t *checks.T) {
}
